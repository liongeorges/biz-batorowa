# ★★ LINE専用資産：絶対にコミットしない ★★
# 「3つの地雷リスク」「最初のミッション」はLINE友だち登録の唯一の対価。
# publicリポジトリに入った瞬間、URLで全公開されて商品価値がゼロになる。
# ⚠️ 一度コミットすると、削除してもGit履歴に残る。最初から入れないのが唯一の対策。
*地雷*
*ミッション*
*回収コピー*
*結果カード_回収*
/line/
/private/
# 環境・秘密
.env
.env.*
*.key
# OS / エディタ
.DS_Store
Thumbs.db
.vscode/
.idea/
# 依存（このプロジェクトでは使わないが保険）
node_modules/
